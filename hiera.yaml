:backends:
  - yaml
:yaml:
  :datadir: "/etc/puppet/hiera/hieradata"
:hierarchy:
  - "%{environment}/hosts/%{hostname}"
  - "%{environment}/modules/%{module_name}"
  - "%{environment}/common"
  - "common"
